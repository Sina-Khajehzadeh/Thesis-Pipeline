{
  "extends": "scenario_xgboost_budget.example.json",
  "enabled": false,
  "template_only": false,
  "experiment_name": "Example_Fixed_Context_Local_TabPFN",
  "models": {
    "TabPFN": {
      "execution": {
        "path": "local",
        "local_device": "cpu",
        "require_requested_device": true
      },
      "local_tabpfn_budget": {
        "context_strategy": {
          "strategy": "fixed",
          "fraction": 0.25,
          "fraction_denominator": "outer_train"
        },
        "minimum_context_budget_multiplier_enabled": true
      }
    },
    "XGBoost": {
      "execution": {
        "device": "cpu",
        "require_requested_device": true
      }
    }
  }
}
